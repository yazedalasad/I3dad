# Security tests

Host **policy and penetration style** checks (RLS, role boundaries, token leakage) documented in **`docs/testing/QA_CHECKLIST.md`** → *Security testing*.

Add automated security suites here when introducing a runner that can hit staging with disposable credentials.
