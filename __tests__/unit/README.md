# Unit tests

Jest specs for **screens, hooks, and pure helpers** live here (Section 6.2 — unit level). Colocated `*.test.js` files elsewhere in the repo remain valid; this folder is the canonical tree for the final project document.

Run only this folder:

```bash
npm test -- __tests__/unit
```
