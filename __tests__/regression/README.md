# Regression smoke

After each bug fix or feature:

1. `npm test`
2. Run the **Regression testing** table in **`docs/testing/QA_CHECKLIST.md`**

Optional: add a tiny Jest file here that imports shared smoke helpers once they exist.
